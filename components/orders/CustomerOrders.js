import { ClipboardList } from 'lucide-react';
import { formatCartPrice } from '../cart/cartUtils';

const labels = { novo: 'Novo', confirmado: 'Confirmado', em_preparo: 'Em preparo', saiu_para_entrega: 'Saiu para entrega', entregue: 'Entregue', cancelado: 'Cancelado' };
export default function CustomerOrders({ orders }) { return <section className="section pt-0"><div className="border-t border-white/10 pt-10"><p className="eyebrow">Compras</p><h2 className="text-2xl font-extrabold">MEUS PEDIDOS</h2>{orders.length ? <div className="mt-6 grid gap-4">{orders.map((order) => <article key={order.id} className="flex flex-wrap items-center justify-between gap-5 rounded-2xl border border-white/10 bg-white/[.025] p-5"><div><p className="font-extrabold">Pedido #{order.order_number}</p><p className="mt-1 text-sm text-zinc-500">{new Intl.DateTimeFormat('pt-BR', { dateStyle: 'medium' }).format(new Date(order.created_at))} · {(order.order_items || []).reduce((total, item) => total + item.quantity, 0)} itens</p></div><div className="flex items-center gap-5"><span className="rounded-full border border-brand/30 bg-brand/10 px-3 py-1 text-xs font-bold text-brand">{labels[order.status] || order.status}</span><strong className="text-brand">{formatCartPrice(order.total)}</strong><a href={`/minha-conta/pedidos/${order.id}`} className="button-dark px-4 py-3">Ver detalhes</a></div></article>)}</div> : <div className="mt-6 rounded-2xl border border-dashed border-white/15 p-8 text-center text-zinc-500"><ClipboardList size={25} className="mx-auto text-brand"/><p className="mt-3 text-sm">Você ainda não realizou nenhum pedido.</p><a href="/produtos" className="mt-5 inline-block text-sm font-bold text-brand">Conhecer produtos</a></div>}</div></section>; }
