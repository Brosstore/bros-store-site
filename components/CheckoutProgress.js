import { CheckCircle2, CreditCard, ShoppingBag, UserRound } from 'lucide-react';
const steps = [[ShoppingBag,'Carrinho'],[UserRound,'Identificação'],[CreditCard,'Pagamento'],[CheckCircle2,'Confirmação']];
export default function CheckoutProgress() { return <nav aria-label="Etapas da compra" className="border-b border-white/10 bg-[#0d0d0d]"><ol className="mx-auto flex max-w-5xl items-center justify-between gap-2 overflow-x-auto px-5 py-4 sm:px-8">{steps.map(([Icon,label],index)=><li key={label} className={`flex shrink-0 items-center gap-2 text-xs font-bold ${index===2?'text-brand':'text-zinc-500'}`} aria-current={index===2?'step':undefined}><span className={`grid h-7 w-7 place-items-center rounded-full border ${index===2?'border-brand bg-brand text-ink':'border-white/15'}`}><Icon size={14}/></span><span className="hidden sm:inline">{label}</span>{index<steps.length-1&&<span className="ml-2 h-px w-6 bg-white/15 sm:w-12"/>}</li>)}</ol></nav>; }
