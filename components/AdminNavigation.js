'use client';
import { Menu, X } from 'lucide-react';
import { usePathname } from 'next/navigation';
import { useState } from 'react';
import AdminLogoutButton from './AdminLogoutButton';
const links = [['/admin/dashboard','Dashboard'],['/admin/produtos','Produtos'],['/admin/categorias','Categorias'],['/admin/pedidos','Pedidos'],['/admin/estoque','Estoque'],['/admin/configuracoes','Configurações']];
export default function AdminNavigation() { const path = usePathname(); const [open, setOpen] = useState(false); if (path === '/admin/login') return null; const item = ([href,label]) => <a key={href} href={href} onClick={() => setOpen(false)} className={`rounded-lg px-3 py-2 text-sm font-bold transition ${path === href || path.startsWith(`${href}/`) ? 'bg-brand text-black' : 'text-zinc-300 hover:bg-white/10 hover:text-brand'}`}>{label}</a>; return <nav className="sticky top-0 z-40 border-b border-white/10 bg-ink/95 px-5 py-3 backdrop-blur lg:px-12"><div className="mx-auto flex max-w-7xl items-center justify-between gap-4"><a href="/admin/dashboard" className="text-base font-extrabold tracking-[.1em]">BROS<span className="ml-1 text-brand">STORE</span></a><div className="hidden items-center gap-1 lg:flex">{links.map(item)}<AdminLogoutButton /></div><button type="button" className="rounded-lg border border-white/15 p-2 lg:hidden" aria-label="Abrir menu administrativo" onClick={() => setOpen(!open)}>{open ? <X size={20}/> : <Menu size={20}/>}</button></div>{open && <div className="mx-auto mt-3 grid max-w-7xl gap-1 border-t border-white/10 pt-3 lg:hidden">{links.map(item)}<AdminLogoutButton /></div>}</nav>; }
