import { ArrowUpRight } from 'lucide-react';
import MotionReveal from './MotionReveal';
import { categories } from '../data/products';

export default function Categories() { return <section id="categorias" className="section"><MotionReveal><div className="mb-11 flex items-end justify-between"><div><p className="eyebrow">Categorias Bros</p><h2 className="section-title">ENCONTRE O SEU<br/> <span className="text-zinc-500">ESTILO.</span></h2></div><span className="hidden font-mono text-xs text-zinc-500 sm:block">[ 06 CATEGORIAS ]</span></div></MotionReveal><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{categories.map(({ num, name, desc, image }, index) => <MotionReveal variant="scale" delay={index * .06} key={name}><a href="#produtos" className="group relative block h-[340px] overflow-hidden rounded-xl md:h-[390px]"><img loading="lazy" src={image} alt={name} className="h-full w-full object-cover grayscale transition duration-700 group-hover:scale-105 group-hover:grayscale-0"/><div className="absolute inset-0 bg-gradient-to-t from-black via-black/20 to-transparent"/><div className="absolute inset-x-0 bottom-0 p-6 sm:p-7"><div className="mb-7 flex items-center justify-between text-[10px] font-mono text-brand"><span>{num}</span><ArrowUpRight size={16}/></div><h3 className="text-3xl font-extrabold tracking-tight">{name}</h3><p className="mt-2 max-w-[14rem] text-sm text-zinc-300">{desc}</p></div></a></MotionReveal>)}</div></section>; }
