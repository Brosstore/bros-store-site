'use client';
import { LoaderCircle, LogIn, Mail } from 'lucide-react';
import { useRouter, useSearchParams } from 'next/navigation';
import { useState } from 'react';
import { authErrorMessage, logAuthError } from '../../lib/authMessages';
import { createClient } from '../../lib/supabase/client';
export default function LoginForm() { const router = useRouter(); const params = useSearchParams(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [recovery,setRecovery]=useState(false); const [message,setMessage]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); async function submit(event) { event.preventDefault(); setError(''); setMessage(''); setLoading(true); try { const supabase=createClient(); if(recovery){ const {error:e}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/login`}); if(e) throw e; setMessage('Enviamos as instruções de recuperação para seu e-mail.'); return; } await supabase.auth.signOut(); const {error:e}=await supabase.auth.signInWithPassword({email,password}); if(e) throw e; const next=params.get('next'); router.replace(next?.startsWith('/')&&!next.startsWith('//')&&(next.startsWith('/checkout')||next.startsWith('/minha-conta')||next.startsWith('/pedido-confirmado/'))?next:'/minha-conta'); router.refresh(); } catch(e) { logAuthError('login-cliente',e); setError(recovery?'Não foi possível enviar as instruções. Confira o e-mail e tente novamente.':authErrorMessage(e)); } finally { setLoading(false); } } return <form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm font-semibold text-zinc-200">E-mail<input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-white/15 bg-black/40 px-4 py-3.5 text-white outline-none focus:border-brand"/></label>{!recovery&&<label className="block text-sm font-semibold text-zinc-200">Senha<input required type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-white/15 bg-black/40 px-4 py-3.5 text-white outline-none focus:border-brand"/></label>}{error&&<p role="alert" className="rounded-lg border border-red-400/30 bg-red-400/10 px-4 py-3 text-sm text-red-200">{error}</p>}{message&&<p role="status" className="rounded-lg border border-brand/30 bg-brand/10 px-4 py-3 text-sm text-brand">{message}</p>}<button disabled={loading} className="button-primary w-full disabled:opacity-70">{loading?<LoaderCircle size={17} className="animate-spin"/>:recovery?<Mail size={17}/>:<LogIn size={17}/>} {loading?'Aguarde...':recovery?'Enviar instruções':'Entrar'}</button><button type="button" onClick={()=>{setRecovery(!recovery);setError('');setMessage('');}} className="text-sm text-zinc-400 hover:text-brand">{recovery?'Voltar ao login':'Esqueci minha senha'}</button></form>; }
