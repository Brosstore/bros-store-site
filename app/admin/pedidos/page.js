import { Eye, Search } from 'lucide-react';
import { redirect } from 'next/navigation';
import { formatCartPrice } from '../../../components/cart/cartUtils';
import { getAdminOrders } from '../../../lib/orders';
import { createClient } from '../../../lib/supabase/server';

export const metadata = { title: 'Pedidos | Painel Bros Store', robots: { index: false, follow: false } };
const labels = { novo: 'Novo', confirmado: 'Confirmado', em_preparo: 'Em preparo', saiu_para_entrega: 'Saiu para entrega', entregue: 'Entregue', cancelado: 'Cancelado' };
export default async function AdminOrdersPage({ searchParams }) { const supabase = createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect('/admin/login'); const filters = { status: typeof searchParams?.status === 'string' ? searchParams.status : '', customer: typeof searchParams?.cliente === 'string' ? searchParams.cliente : '', number: typeof searchParams?.numero === 'string' ? searchParams.numero : '', from: typeof searchParams?.de === 'string' ? searchParams.de : '', to: typeof searchParams?.ate === 'string' ? searchParams.ate : '' }; const orders = await getAdminOrders(filters); return <main className="min-h-screen bg-ink px-5 py-8 text-white sm:px-8 lg:px-12"><div className="mx-auto max-w-7xl"><header className="flex flex-wrap items-center justify-between gap-5 border-b border-white/10 pb-7"><div><a href="/admin/dashboard" className="text-xl font-extrabold tracking-[.08em]">BROS<span className="ml-1 text-brand">STORE</span></a><p className="mt-2 text-sm text-zinc-400">Painel administrativo / Pedidos</p></div><a href="/admin/produtos" className="button-dark">Produtos</a></header><section className="py-10"><p className="eyebrow">Operação</p><h1 className="text-4xl font-extrabold tracking-tight sm:text-5xl">Pedidos</h1><p className="mt-3 text-sm text-zinc-400">{orders.length} pedido{orders.length === 1 ? '' : 's'} encontrado{orders.length === 1 ? '' : 's'}.</p><form className="mt-8 grid gap-3 rounded-2xl border border-white/10 bg-white/[.03] p-4 md:grid-cols-2 xl:grid-cols-[1fr_1fr_11rem_10rem_10rem_auto]"><label className="relative"><span className="sr-only">Número do pedido</span><Search size={17} className="pointer-events-none absolute left-4 top-1/2 -translate-y-1/2 text-zinc-500"/><input name="numero" defaultValue={filters.number} placeholder="Nº do pedido" className="w-full rounded-xl border border-white/15 bg-black/30 py-3 pl-11 pr-4 text-sm outline-none focus:border-brand"/></label><input name="cliente" defaultValue={filters.customer} placeholder="Cliente" className="rounded-xl border border-white/15 bg-black/30 px-4 py-3 text-sm outline-none focus:border-brand"/><select name="status" defaultValue={filters.status} className="rounded-xl border border-white/15 bg-black/30 px-4 py-3 text-sm outline-none focus:border-brand"><option value="">Todos os status</option>{Object.entries(labels).map(([value,label]) => <option key={value} value={value}>{label}</option>)}</select><input name="de" type="date" defaultValue={filters.from} aria-label="Data inicial" className="rounded-xl border border-white/15 bg-black/30 px-4 py-3 text-sm outline-none focus:border-brand"/><input name="ate" type="date" defaultValue={filters.to} aria-label="Data final" className="rounded-xl border border-white/15 bg-black/30 px-4 py-3 text-sm outline-none focus:border-brand"/><button className="button-primary px-5 py-3">Filtrar</button></form><div className="mt-6 overflow-x-auto rounded-2xl border border-white/10"><table className="min-w-[900px] w-full text-left text-sm"><thead className="bg-white/[.04] text-[11px] font-bold uppercase tracking-wider text-zinc-400"><tr><th className="px-5 py-4">Nº</th><th className="px-5 py-4">Cliente</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Pagamento</th><th className="px-5 py-4">Total</th><th className="px-5 py-4">Data</th><th className="px-5 py-4 text-right">Ações</th></tr></thead><tbody>{orders.map((order) => <tr key={order.id} className="border-t border-white/10 hover:bg-white/[.025]"><td className="px-5 py-4 font-bold">#{order.order_number}</td><td className="px-5 py-4">{order.customer ? `${order.customer.nome} ${order.customer.sobrenome}`.trim() : 'Cliente não identificado'}</td><td className="px-5 py-4"><span className="rounded-full border border-brand/30 bg-brand/10 px-3 py-1 text-xs font-bold text-brand">{labels[order.status] || order.status}</span></td><td className="px-5 py-4">{order.payment_method === 'cartao_na_entrega' ? 'Cartão na entrega' : order.payment_method}</td><td className="px-5 py-4 font-bold text-brand">{formatCartPrice(order.total)}</td><td className="px-5 py-4 text-zinc-400">{new Intl.DateTimeFormat('pt-BR', { dateStyle: 'short', timeStyle: 'short' }).format(new Date(order.created_at))}</td><td className="px-5 py-4 text-right"><a href={`/admin/pedidos/${order.id}`} className="rounded-lg border border-white/15 px-3 py-2 text-xs font-bold transition hover:border-brand hover:text-brand"><Eye size={14} className="mr-1 inline"/>Detalhes</a></td></tr>)}</tbody></table>{!orders.length && <p className="px-5 py-10 text-center text-zinc-500">Nenhum pedido encontrado para estes filtros.</p>}</div></section></div></main>; }
