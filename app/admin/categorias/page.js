import { redirect } from 'next/navigation';
import { createClient } from '../../../lib/supabase/server';
import CategoryManager from './CategoryManager';
export const metadata={title:'Categorias | Painel Bros Store',robots:{index:false,follow:false}};
export default async function CategoriesAdmin(){const supabase=createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/admin/login');const {data:admin}=await supabase.from('admin_users').select('user_id').eq('user_id',user.id).maybeSingle();if(!admin)redirect('/admin/dashboard');const {data,error}=await supabase.from('categories').select('*').order('sort_order');return <main className="min-h-screen bg-ink px-5 py-8 text-white sm:px-8 lg:px-12"><div className="mx-auto max-w-6xl"><header className="border-b border-white/10 pb-7"><a href="/admin/dashboard" className="text-xl font-extrabold">BROS<span className="text-brand"> STORE</span></a><p className="mt-2 text-sm text-zinc-400">Painel administrativo / Categorias</p></header><section className="py-10"><p className="eyebrow">Catálogo</p><h1 className="text-4xl font-extrabold">Categorias</h1>{error?<p className="mt-5 text-red-200">Aplique a migration de categorias antes de gerenciar.</p>:<CategoryManager initialCategories={data||[]}/>}</section></div></main>}
