# Bros Store

Site institucional e catálogo responsivo da Bros Store, desenvolvido em Next.js e Tailwind CSS.

## Como executar

```bash
npm install
npm run dev
```

Abra `http://localhost:3000` no navegador.

> Atualize o número `5500000000000` nos componentes para o WhatsApp comercial da loja antes da publicação.
